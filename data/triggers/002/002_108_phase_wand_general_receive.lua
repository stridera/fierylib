-- Trigger: phase wand general receive
-- Zone: 2, ID: 108
-- Type: MOB, Flags: RECEIVE
--
-- Generic receive handler for every wand questmaster from wandstep 3-10.
-- Reads per-mob crafting parameters from globals (set by 002_111). When
-- handed an expected item it advances the matching <type>_wand quest,
-- destroys the input, spawns the next-tier reward, and awards exp.
--
-- TODO(parity): Several converter artifacts cannot be fixed mechanically:
--   * `object.id == "wandgem"` etc. compare an integer object id against
--     a literal string — these checks always fail. The original DG used
--     `if %object.vnum%==%wandgem%` which referenced the per-mob gem id
--     loaded by 002_111, so the runtime needs to compare object.id (or
--     the (zone,id) composite) against globals.wandgem / globals.wand_id /
--     globals.wandtask3 / globals.wandtask4 etc.
--   * `actor.has_completed[type_wand]` and `get.room[wandtask4]` are DG
--     index reads against undefined globals; these must be rewritten as
--     `actor:get_has_completed(quest)` and `get_room(z, id)`.
--   * Branch-scoped `local response` / `local check` / `local continue`
--     don't escape their elseif blocks, so the dispatch at the end of the
--     trigger always sees nil. The dispatch logic itself is correct in
--     intent but needs the variables hoisted to function scope.
--   * Embedded `%get.obj_shortdesc[%wandtask3%]%` interpolations need to
--     be replaced with `objects.template(z, id).name`.
--   * `self.room:spawn_object(math.floor(wandtask4 / 100), wandtask4 % 100)`
--     uses CircleMUD vnum split semantics; once globals exports proper
--     (zone, id) pairs this should pass them directly.
--   * The exp-multiplier block is correct other than relying on a hoisted
--     `expmod` (currently shadowed by the per-branch `local`).
--
-- Until those issues are resolved (they require the engine's quest-item
-- table to be exposed to Lua), this trigger no-ops so partial conversions
-- don't soft-lock the wand quest at stage 3+.
local _return_value = true
return _return_value
